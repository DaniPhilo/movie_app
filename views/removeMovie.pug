doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
        link(rel="stylesheet", href="https://unpkg.com/boxicons@2.1.2/css/boxicons.min.css")
        link(rel="stylesheet", href="../css/styles.css")
    body 
        header 
            nav(class = "navbar")
                a(class = "nav__branding") THE MOVIES APP 
                ul(class = "nav__menu" id = "nav__menu__admin")
                    li(class = "nav__item" id = "nav__item")
                        p(href = "#Cerrar sesión", class = "nav__link" onclick="logOut()")  Cerrar Sesión
                            i(class = "bx bx-log-out")
                div(class = "hamburguer")
                    span.bar 
                    span.bar 
                    span.bar
    main
        if success 
            p #{success.message}
            a(href='http://localhost:3000/admin') Go back
                i(class='bx bx-arrow-back')

        if movie

            a(href="/admin") 
                i(class='bx bx-arrow-back', id = "back__button")
            h2 ¿Estás seguro que deseas eliminar esta pelicula?
                
            form.films__section(action=`/removeMovie/${movie.titulo}` method="POST") 
                div.film__container
                    img.film__img(src=`${movie.imagen}`)
                    div.film__texts
                        h3 Titulo: #{movie.titulo}
                        p Año: #{movie.year}
                        p Director: #{movie.director}
                        p Genero: #{movie.genero}
                        p Duracion: #{movie.duracion}
                    div.film__buttons
                        input(type="submit" value="Eliminar")
                

    script(src="/js/burger.js")
